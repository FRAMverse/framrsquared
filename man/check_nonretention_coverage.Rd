% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_fishery_coverage.R
\name{check_nonretention_coverage}
\alias{check_nonretention_coverage}
\title{Check each fishery x timestep for non-retention}
\usage{
check_nonretention_coverage(fram_db, run_id)
}
\arguments{
\item{fram_db}{fram database connection}

\item{run_id}{FRAM run id(s)}
}
\value{
dataframe with fishery, timestep, run, and \verb{$has_nr}, which is TRUE if there
is non-zero non-retention modeled, FALSE otherwise.
}
\description{
For one or more run ids, uses NonRetention table of FRAM database to identify which
fishery x timesteps have non-retention applied. Uses flagging rules to identify \emph{actual}
non-retention used; to check that there are no mismatches between nonretention flags
and nonretention values, use \code{\link[=check_nonretention_flagging]{check_nonretention_flagging()}}. \code{check_nonretention_coverage()} assumes
any non-modeled fishery has no non-retnention. Consequently, fishery x timesteps
that are not included in the base period will show up as \code{FALSE} for nonretention.
}
\examples{
\dontrun{
fram_db |> check_nonretention_coverage(run_id = 47)
}
}
\seealso{
\code{\link[=check_nonretention_flagging]{check_nonretention_flagging()}}, \code{\link[=plot_nonretention_coverage]{plot_nonretention_coverage()}}
}

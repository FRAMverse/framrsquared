% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_stock_check.R
\name{addstock_check}
\alias{addstock_check}
\title{Check FRAM database after adding new stock}
\usage{
addstock_check(
  file_name = NULL,
  run_id = NULL,
  old_stockcount = 78,
  override_db_checks = FALSE
)
}
\arguments{
\item{file_name}{filepath to database. If \code{NULL}, provide summary of process instead. Default = \code{NULL}.}

\item{run_id}{RunID associated with the new stock in the FRAM database. If left as \code{NULL}, provide summary of process instead. Default = \code{NULL}.}

\item{old_stockcount}{The number of stocks previously present to treat as the "baseline" -- several checking steps will focus solely on newly added stocks. Defaults to 78.}

\item{override_db_checks}{Ignore species, database type. When \code{FALSE}, function will stop if the database is not Chinook or if it's a transfer file. Defaults to FALSE.}
}
\description{
Either provides the step by step process of adding new stock to a FRAM database, or walks through fram database run
and checks the tables for potential errors associated with adding new stock.
}
\examples{
\dontrun{
## review process
addstock_check()
## check database for additional stock
addstock_check("2024 Pre-Season Chinook DB - first test.mdb",
run_id = 138)
}
}

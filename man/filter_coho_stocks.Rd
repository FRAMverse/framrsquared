% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters.R
\name{filter_coho_stocks}
\alias{filter_coho_stocks}
\title{Primary stock filtering function}
\usage{
filter_coho_stocks(.data, filter_criterion, species = NULL, return_ids = FALSE)
}
\arguments{
\item{.data}{dataframe that contains a \code{stock_id} column.}

\item{filter_criterion}{identifier for the stock category, based on the \code{coho_stock_marlene}
internal. Individual functions call appropriate search categories.}

\item{species}{"COHO" or "CHINOOK" (or their permutations) to identify which species' filters
should be applied. Character atomic, optional, only needed if dataframe doesn't have a
\code{species} attribute (dataframes made by this package typically have that).}

\item{return_ids}{Should the function instead return the stock_ids used to make this filter?
Logical, defaults to FALSE.}
}
\value{
A dataframe filtered based on a \code{stock_id} column. If \code{return_ids = TRUE}, instead
returns numeric vector of the fishery IDs used to filter.
}
\description{
Applies the specified stock filter criterion to a dataframe, making it possible to write simpler
individual filter functions. Consider using this for a rewrite fo the fisheries filter criterion
used above.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkfram_diagnostics.R
\name{aggregate_bkfram_check}
\alias{aggregate_bkfram_check}
\title{Aggregates data from a bkfram check file to the joint marked-unmarked coho stock level.}
\usage{
aggregate_bkfram_check(data)
}
\arguments{
\item{data}{Tibble, output of  \code{\link[=parse_bkfram_check]{parse_bkfram_check()}}}
}
\value{
Tibble with columns based on bkfram check file.
Additional columns: \code{missing_start_cohort}:
\itemize{
\item \verb{$missing_start_cohort}: logical, TRUE if both marked and unmarked components have NAs for starting cohorts (useful for filtering out stocks that aren't being solved for)
\item \verb{$stock_aggregate_name}: \code{stock_name} without the "M-" or "U-" prefix
\item \verb{$stock_id} and \verb{$stock_name}: list-columns of 2-d vectors with both stock_id and stock_name values for each combined stock. Can be \code{unnest}'d to make lookup table (see example)
}
}
\description{
Only works for Coho!
}
\examples{
\dontrun{
data <- parse_bkfram_check(system.file("BackFramCheck.Txt", package = "framrsquared.dev"))
data <- aggregate_bkfram_check(data)
lut <- out |>
 dplyr::select(stock_name, stock_id, stock_aggregate_id, stock_aggregate_name) |>
 dplyr::distinct() |>
 tidyr::unnest(cols = c("stock_name", "stock_id"))
 }
}

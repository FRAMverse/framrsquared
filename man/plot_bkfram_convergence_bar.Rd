% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkfram_diagnostics.R
\name{plot_bkfram_convergence_bar}
\alias{plot_bkfram_convergence_bar}
\title{Barplots of stocks that fail to converge by iteration X}
\usage{
plot_bkfram_convergence_bar(
  filepath,
  iteration = NULL,
  plot_ratio = TRUE,
  aggregate_stocks = TRUE,
  thresh = 0.01,
  verbose = TRUE
)
}
\arguments{
\item{filepath}{Full filepath to "BackFramCheck.Txt"}

\item{iteration}{What iteration should we plot? If not provided, plot final iteration. Numeric, defaults to \code{NULL}}

\item{plot_ratio}{Should the filtering and plotting be based on ratios (\code{TRUE}) or total fish (\code{FALSE}). See details. Logical, defaults to \code{TRUE}.}

\item{aggregate_stocks}{Should marked and unmarked stocks be combined? Logical, defaults to \code{TRUE}.}

\item{thresh}{Threshold. If \code{plot_ratio == TRUE}, a thresh of 0.01 filters out stocks with ratios target/modeled escapement of 0.99 to 1.01. If \code{plot_ratio == FALSE}, a thresh of 0.01 filters out stocks with target - modeled escapements of -0.01 to 0.01. Numeric, defaults to 0.01}

\item{verbose}{Print optional statements to CLI?}
}
\value{
ggplot2 object.
}
\description{
Filters to stocks whose model escapement at iteration of interest are not within \code{thresh} of the target ratio (or target value),
and creates a barplot of how much these stocks differ from the target ratio/value.
}
\details{
By default, filters and plots based on the target ratio (e.g., \verb{(target escapement)/(model escapement)}, where
a threshold of 0.01 means filtering out stocks with ratios between 0.99 and 1.01. Optionally can instead
filter and plot based on the difference in fish between target and model escapements.
}
\examples{
 plot_bkfram_convergence_bar(system.file("BaseFramCheck.Txt",
                             package = "framrsquared.dev"),
                             iteration = 20)
}
\seealso{
\code{\link[=plot_bkfram_convergence_trace]{plot_bkfram_convergence_trace()}}, \code{\link[=plot_bkfram_convergence_scatter]{plot_bkfram_convergence_scatter()}}
}

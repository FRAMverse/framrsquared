% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkfram_diagnostics.R
\name{process_bkfram_check}
\alias{process_bkfram_check}
\title{Read and process backwards FRAM check file}
\usage{
process_bkfram_check(filepath, stock_id = NULL, aggregate_stocks = TRUE)
}
\arguments{
\item{filepath}{Full filepath for \code{BackwardFramCheck.Txt} file. Character.}

\item{stock_id}{One or more Coho stock IDs to filter to. Numeric, optional, defaults to \code{NULL}.}

\item{aggregate_stocks}{Should}
}
\value{
Tibble; columns depend on whether aggregation was performed or not, but always returns these key columns:
\itemize{
\item \verb{$iteration}: fram iteration
\item \verb{$escapement}: model escapement for stock or stock aggregrate
\item \verb{$escapement_target}: corresponding target escamenet
\item \verb{$esc_target_ratio}: target/model escapement
\item \verb{$stock_label}: stock name and id(s), depending if aggregated or not.
}
}
\description{
Parses and processes the "BackwardFramCheck.Txt" output of a backwards Coho FRAM run.
Primarily intended to support plotting functions. Adds "stock_label" based on short stock name
and stock id in "BackwardFramCheck.Txt".
Optionally aggregates marked and unmarked stocks together (argument \code{aggregate_stocks}),
Optionally filters to one or more stock ids (argument \code{stock_id}).
}
\examples{
process_bkfram_check(system.file("BaseFramCheck.Txt",
                      package = "framrsquared.dev"),
                      aggregate_stocks = FALSE)
process_bkfram_check(system.file("BaseFramCheck.Txt",
                     package = "framrsquared.dev"),
                     aggregate_stocks = TRUE)
}

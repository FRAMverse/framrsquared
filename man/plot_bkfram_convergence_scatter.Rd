% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkfram_diagnostics.R
\name{plot_bkfram_convergence_scatter}
\alias{plot_bkfram_convergence_scatter}
\title{Barplots of stocks that fail to converge by iteration X}
\usage{
plot_bkfram_convergence_scatter(
  filepath,
  iteration = NULL,
  aggregate_stocks = TRUE,
  verbose = TRUE,
  thresh = 0.01,
  max_n = 10,
  label_size = 4
)
}
\arguments{
\item{filepath}{Full filepath to "BackFramCheck.Txt"}

\item{iteration}{What iteration should we plot? If not provided, plot final iteration. Numeric, defaults to \code{NULL}}

\item{aggregate_stocks}{Should marked and unmarked stocks be combined? Logical, defaults to \code{TRUE}.}

\item{verbose}{Print optional statements to CLI?}

\item{thresh}{Threshold. If \code{plot_ratio == TRUE}, a thresh of 0.01 filters out stocks with ratios target/modeled escapement of 0.99 to 1.01. If \code{plot_ratio == FALSE}, a thresh of 0.01 filters out stocks with target - modeled escapements of -0.01 to 0.01. Numeric, defaults to 0.01}

\item{max_n}{Maximum number of stocks to plot. Numeric, defaults to 10.}

\item{label_size}{Size of label text; may want to adjust for readability based on plot size. Numeric, defaults to 5.}
}
\value{
ggplot2 object
}
\description{
Filters to stocks whose model escapement at iteration of interest are not within \code{thresh} of the target ratio (or target value),
and creates a scatterplot of target vs modeled escapement. If many stocks would be plotted, filters to the most extreme \code{max_n} of them.
}
\examples{
 plot_bkfram_convergence_bar(system.file("BackFramCheck.Txt",
                                         package = "framrsquared.dev"),
                                         iteration = 20)
}
\seealso{
\code{\link[=plot_bkfram_convergence_trace]{plot_bkfram_convergence_trace()}}, \code{\link[=plot_bkfram_convergence_bar]{plot_bkfram_convergence_bar()}}
}

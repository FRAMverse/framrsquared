% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_trs.R
\name{calculate_report_trs}
\alias{calculate_report_trs}
\title{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Calculate Terminal Run Sizes (for COHO reporting)}
\usage{
calculate_report_trs(fram_db, run_id, trs_definition_number = NULL)
}
\arguments{
\item{fram_db}{FRAM database connection}

\item{run_id}{One or more run ids}

\item{trs_definition_number}{Optional. One or more identifiers for terminal run groups (\code{TaaNum} of TAAETRSList for \code{calculate_report_trs()} or \code{Option5} of ReportDriver for \code{calculate_tami_trs()}). If provided, results will be filtered to only those terminal run groups.}
}
\value{
A data frame with one row per stock group per run, containing the
following columns:
\describe{
\item{\code{run_id}}{Integer. Unique identifier for the model run.}
\item{\code{taa_num}}{Numeric. Identifier for the terminal run size record. Matches \code{Option6} of FRAM table "ReportDriver".}
\item{\code{taa_name}}{Character. Name of the stock group. Corresponds to \code{Option5} of FRAM table "ReportDriver".}
\item{\code{escapement}}{Numeric. Escapement estimate for the stock group. Already incorporated in \code{terminal_run_size}, provided as additional reference.}
\item{\code{terminal_run_size}}{Numeric. Final terminal run size value, either TAA or ETRS depending on the run type.}
\item{\code{terminal_run_type}}{Character. Either "ETRS" or "TAA", identifying what type of terminal run definition is being used.}
\item{\code{terminal_run_category}}{Reminder that this is the Reporting version of terminal runs, NOT the version used by the TAMI --> FRAM process}
}
}
\description{
Calculate the Terminal Area Abundance (TAA) and Extreme Terminal Run Size (ETRS) values used in COHO model output reporting. These match the values of column B of the \code{TRunsPRN} sheet of the TAMM. \strong{WARNING!} These are NOT the same terminal run size definitions used by FRAM to interpret the TAMI rate inputs (for that, see \code{\link[=calculate_tami_trs]{calculate_tami_trs()}}).
}
\details{
Terminal runs are defined in the "ReportDriver" table of the FRAM database. Terminal run name ("Option5") and terminal run number ("Option6") are used in combination to uniquely identify the terminal run group in these calculations. The FRAM database does not guarantee uniqueness of these combinations, and \code{calculate_report_trs()} will error if there are multiple rows of "ReportDriver" with duplicate Option5 x Option6. \code{calculate_report_trs()} \emph{will} correctly produce output if there are duplicates in one or the other columns of the table; in this case the output of this function may have two rows with the same \verb{$trs_id} or \verb{$stock_group_name}. In the event of multiple rows with the same \verb{$stock_group_name}, this function will provide a warning. This is the case for many of the long-standing databases. Be careful when using the output of this function; do not assume that \verb{$trs_id} or \verb{$stock_group_name} alone will uniquely identify a single row of output.
}
\examples{
\dontrun{
fram_db <- connect_fram_db("coho_postseason.mdb")
calculate_report_trs(fram_db, run_id = 51:53)
## What if we just want the HC Unmarked ETRS? ReportDriver table shows that's 73
calculate_report_trs(fram_db, run_id = 51:53,
    trs_definition_number = 73)
}
}
\seealso{
Other calculate terminal run size:
\code{\link[=calculate_tami_trs]{calculate_tami_trs()}}
}
\concept{calculate terminal run size}

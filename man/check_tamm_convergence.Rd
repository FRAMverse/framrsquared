% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tamm_convergence.R
\name{check_tamm_convergence}
\alias{check_tamm_convergence}
\title{Check TAMM iteration convergence}
\usage{
check_tamm_convergence(filepath, threshold_fish = 20, quiet = FALSE)
}
\arguments{
\item{filepath}{Path to a \code{FramCheck.txt} file.}

\item{threshold_fish}{Numeric scalar. Minimum absolute change in fish (or
quota) between the last two iterations to flag as a potential convergence
problem. Defaults to \code{20}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses console output. Defaults to
\code{FALSE}.}
}
\value{
Invisibly, a named list with elements:
\describe{
\item{trs_diff}{Data frame of per-TRS change and slope between the final two iterations.}
\item{taa_etrs_diff}{Data frame of per-fishery quota change and slope between the final two iterations.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Evaluates how much terminal run sizes (TRS) and TAA/ETRS quotas changed
between the final two TAMM iterations in a \code{FramCheck.txt} file. Optionally
prints a summary to the console. Currently only implemented for Coho.

For both of the ouput objects, the "diffs" are based on final two iterations, with \verb{$change_in_fish} and \verb{$change_in_quota} giving the number in raw fish. \code{slope} is calculated as the difference in the final two counts (TRS or abundance) divided by their average. \code{trs_diff$trs_id} corresponds to the \code{TaaNum} column of the \code{TAAETRSList} table in the FRAM database.
}
\examples{
\dontrun{
check_tamm_convergence("path/to/FramCheck.txt", threshold_fish = 10)
}
}
\seealso{
Other tamm convergence:
\code{\link[=parse_fram_check]{parse_fram_check()}},
\code{\link[=plot_tamm_convergence_taa]{plot_tamm_convergence_taa()}},
\code{\link[=plot_tamm_convergence_trs]{plot_tamm_convergence_trs()}}
}
\concept{tamm convergence}

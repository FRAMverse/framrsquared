% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tamm_convergence.R
\name{check_tamm_convergence}
\alias{check_tamm_convergence}
\title{Check TAMM iteration convergence}
\usage{
check_tamm_convergence(filepath, threshold_fish = 20, quiet = FALSE)
}
\arguments{
\item{filepath}{Path to a \code{FramCheck.txt} file.}

\item{threshold_fish}{Numeric scalar. Only used for Coho. Minimum absolute change in fish (or
quota) between the last two iterations to flag as a potential convergence
problem. Defaults to \code{20}.}

\item{quiet}{Logical. Only used for Coho. If \code{TRUE}, suppresses console output. Defaults to
\code{FALSE}.}
}
\value{
For Coho, Invisibly, a named list with elements:
\describe{
\item{trs_diff}{Data frame of per-TRS change and relative_change between the final two iterations.}
\item{taa_etrs_diff}{Data frame of per-fishery quota change and relative_change between the final two iterations.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Evaluates whether TAMM iterations appear to have converged. For Coho, checks how much terminal run sizes (TRS) and TAA/ETRS quotas changed
between the final two TAMM iterations in a \code{FramCheck.txt} file. For Chinook, compares total iteration count with (expected) FRAM maximum and flags any stocks with negative escapements. Prints informative messages to console; for Coho, messages can optionally be surpressed.

For Coho, outputs "diffs" based on final two iterations, with \verb{$change_in_fish} and \verb{$change_in_quota} giving the number in raw fish. \code{relative_change} is calculated as the difference in the final two counts (TRS or abundance) divided by their average. \code{trs_diff$trs_id} corresponds to the \code{TaaNum} column of the \code{TAAETRSList} table in the FRAM database.

For Chinook, the observed number of TAMM iterations is compared against the hard-coded maximum of 15 (FRAM version 2.24). In the event that
you are using an experimental version of FRAM or future versions of FRAM with a different maximum number of Chinook TAMM iterations,
you will need to evaluate the number of TAMM iterations with this in mind.
}
\examples{
\dontrun{
check_tamm_convergence("path/to/FramCheck.txt", threshold_fish = 10)
}

}
\seealso{
Other tamm convergence:
\code{\link[=parse_fram_check]{parse_fram_check()}},
\code{\link[=plot_tamm_convergence_taa]{plot_tamm_convergence_taa()}},
\code{\link[=plot_tamm_convergence_trs]{plot_tamm_convergence_trs()}}
}
\concept{tamm convergence}

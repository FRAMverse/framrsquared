# Identifies the FRAM database type - Full or Transfer

Identifies the FRAM database type - Full or Transfer

## Usage

``` r
fram_database_type(con)
```

## Arguments

- con:

  Connection to FRAM database

## Examples

``` r
if (FALSE) fram_database_type(con) # \dontrun{}
```

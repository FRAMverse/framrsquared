# Removes a run in a FRAM database

Removes a run in a FRAM database

## Usage

``` r
remove_run(fram_db, run_id)
```

## Arguments

- fram_db:

  FRAM database object

- run_id:

  FRAM run ID or IDs to be deleted

## Examples

``` r
if (FALSE) fram_db |> delete_run(run_id = 132) # \dontrun{}
```

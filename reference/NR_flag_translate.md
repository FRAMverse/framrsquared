# Provides English translation of numeric non-retention flags

Provides English translation of numeric non-retention flags

## Usage

``` r
NR_flag_translate(vec)
```

## Arguments

- vec:

  vector of flags

## Examples

``` r
if (FALSE) NR_flag_translate(sample(1:4, 10, replace = T)) # \dontrun{}
```

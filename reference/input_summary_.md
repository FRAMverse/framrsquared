# Generates an input summary based on a FisheryScalers dataframe. Probably end up streamlining / revising this.

Generates an input summary based on a FisheryScalers dataframe. Probably
end up streamlining / revising this.

## Usage

``` r
input_summary_(.data, run_id)
```

## Arguments

- .data:

  FisheryFishery scalers dataframe

- run_id:

  Run ID number

## Examples

``` r
if (FALSE) fishery_scalers_dataframe |> input_summary() # \dontrun{}
```
